#include<bits/stdc++.h>
using namespace std;
#define pb push_back
#define po pop_back
int main()
{
	
	int n,k,s=0;;
	cin>>n>>k;
	int a[n];
	for(int i=0; i < n; i++) 
		cin>>a[i];
	int p = a[k-1];
	for(int i=0; i < n; i++) 
		if(a[i] >= p && a[i] != 0) s++;
	cout<<s<<endl;
	
	

}
