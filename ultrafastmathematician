#include<bits/stdc++.h>
using namespace std;
int main()
{
        string a,b,c;
        cin>>a>>b;
        for(int i=0;i<a.length();i++)
        {
                if(a[i] == b[i]) b[i]='0';
                else b[i] = '1';
        }
        cout<<b;
        return 0;
}
